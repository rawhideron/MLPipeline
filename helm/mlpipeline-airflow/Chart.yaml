apiVersion: v2
name: mlpipeline-airflow
description: Apache Airflow Helm chart for MLPipeline
type: application
version: 1.0.0
appVersion: "2.8.0"

dependencies:
  - name: postgresql
    version: "13.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
